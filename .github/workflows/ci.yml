name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22.x'
      - run: VBUILD_VAR_VERSION=ci go run ./cmd/vbuild test
      - run: VBUILD_VAR_VERSION=ci go run ./cmd/vbuild build
